<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Responsibility Index</title>
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@1.5.7/css/pico.min.css">
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your CSS file -->
</head>
<body>
    <header>
        <h1>Social Responsibility Index</h1>
    </header>
    
    <main class="container">
        <div class="grid">
            <% companies.forEach(company => { %>
                <article class="card">
                    <header>
                        <img src="<%= company.logo %>" alt="<%= company.name %> logo" width="100" class="company-logo">
                        <h2><%= company.name %></h2>
                    </header>
                    <p><%= company.mission %></p>
                    <p><strong>Lobbying Spend:</strong> $<%= company.lobbyingSpend.toLocaleString() %></p>
                    <footer>
                        <a href="/company/<%= company.id %>" role="button" class="secondary">View Details</a>
                    </footer>
                </article>
            <% }); %>
        </div>
    </main>
</body>
</html>


